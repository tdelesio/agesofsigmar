import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.agesofsigmar',
  appName: 'Spearhead Helper ',
  "webDir": "out",
  "server":{
    "androidScheme": "https"
  }
};

export default config;
